const SendActivationCode = require('../Functions/Util/SendActivationCode');

describe('Testing activation code sender function', () => {
    it('Rejects with code 404 if user is not found', async () => {

    });
})