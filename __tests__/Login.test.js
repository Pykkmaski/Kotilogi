const Login = require('../Functions/Util/CreateLoginPayload');

describe('Testing the back-end login function', () => {

   jest.mock('./__mocks__/dbconfig.js');
   it('Rejects with code 404 when trying to log in as a non existent user', () => {
        
   })
});