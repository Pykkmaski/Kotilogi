import { CSSProperties } from "react"

export function ItemList({direction = 'vertical', ...props}: {
    items: any[],
    direction: 'vertical' | 'horizontal',
}){
    const itemListStyle: CSSProperties = {
        display: 'flex',

    }

    return (

    )
}