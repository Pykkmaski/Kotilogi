import '../../scss/LoadingSpinner.scss';

function LoadingSpinner({width, height}){

    return (
        <div className="loading-spinner" style={{width, height}}>

        </div>
    );
}

export default LoadingSpinner;