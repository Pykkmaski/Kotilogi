const RouteHandleError = require('../RouteHandleError');

describe('Testing the route error handling', () => {

    it('Returns a non-string error as a number', () => {
        
    });

    it('Returns a non-number error as a string', () => {

    })
});