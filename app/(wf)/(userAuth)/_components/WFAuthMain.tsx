/**Renders the main-tag of auth-pages. */
export function WFAuthMain({ children }) {
  return (
    <main className='flex w-full h-full items-center justify-center relative z-20'>{children}</main>
  );
}
