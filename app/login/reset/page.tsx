import ResetForm from "./ResetForm";
import style from '../page.module.scss';

export default function ResetPage(){
    return (
        <main className='flex-column flex-full center-all'>
            <ResetForm/>
        </main>
    );
}