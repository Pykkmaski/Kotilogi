import { LabelGrid } from '@/components/New/LabelGrid';

export const DataContent = ({ mainTypeId, extraData }) => {
  const getContent = () => {
    if(mainTypeId == )
  }
  return <div className='flex flex-col gap-4'>

  </div>;
};
