import { FormControl, Input, NullOption } from '@/components/UI/FormUtils';
import { TargetSelector } from './TargetSelector';

export const ElectricityEventContent = () => (
  <>
    <TargetSelector />
  </>
);
